export const enum TowerflowProjectType {
  WebApp = "web-app",
  WebLib = "web-lib",
  NodeApp = "node-app",
  NodeLib = "node-lib",
}

export enum BuildType {
  production = "production",
  dev = "development",
}
